const form = document.querySelector("#form-speciale");

form.addEventListener("submit", gestisciInvioForm);

function gestisciInvioForm(event) {
    event.preventDefault();

    // alert("invio form");
}